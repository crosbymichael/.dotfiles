[Unit]
Description=starbound server
After=docker.service

[Service]
ExecStart=/usr/local/bin/docker start -a starbound
ExecStop=/usr/local/bin/docker stop -t 10 starbound
Restart=on-failure
RestartSec=12

[Install]
WantedBy=multi-user.target
